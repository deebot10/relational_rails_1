<h1>Artists Index Page</h1>
<h2><a href='/artists/new'>Create Artist</a><h2> 

<% @artists.each do |artist| %>
  <h3><%= artist.name %></h3>
  <p>Created at:<%= artist.created_at%></p>
  <%= button_to "Update #{artist.name}", "/artists/#{artist.id}/edit", method: :get%>
<% end %>